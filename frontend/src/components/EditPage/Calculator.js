import React from 'react';
/*
      if (math.operator && math.second) {
        const first  = parseFloat(math.first );
        const second = parseFloat(math.second);
        switch (math.operator) {
          case '+':
            return first + second;
          case '-':
            return first - second;
          case '*':
            return first * second;
          case '/':
            if (second === 0) {
              return first;
            } else {
              return first / second;
            }
        }
      } else {
        return parseFloat('0' + math.first);
      }

*/
const Calculator = () => {
  return (
    <div>
      
    </div>
  );
};

export default Calculator;
